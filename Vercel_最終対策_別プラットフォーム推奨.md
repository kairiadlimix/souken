# Vercelサイズ制限 最終対策

## 問題
VercelのServerless Functionsのサイズ制限（250MB）を超え続けています。

## 根本原因
`FastAPI`とその依存パッケージ（`pydantic`、`starlette`など）が非常に大きく、250MBを超えています。

## 実施した対策
1. ✅ `pdfplumber`を削除
2. ✅ `FastAPI`をダウングレード
3. ✅ `pydantic`をv1に変更
4. ✅ 多くのファイルを除外
5. ✅ 最小限のHTTPハンドラーを作成（`api/index_minimal.py`）

## 推奨解決策

### オプション1: Railwayを使用（推奨）
Railwayはより柔軟なサイズ制限があり、FastAPIをそのまま使用できます。

**メリット**:
- FastAPIをそのまま使用可能
- ファイルアップロードが簡単
- デプロイが簡単
- 無料プランあり

**デプロイ手順**:
1. https://railway.app にアカウント作成
2. GitHubリポジトリを接続
3. 自動的にデプロイ

詳細は `RAILWAY_デプロイ手順.md` を参照してください。

### オプション2: Renderを使用
RenderもFastAPIをサポートしており、サイズ制限が緩いです。

**メリット**:
- FastAPIをそのまま使用可能
- 無料プランあり
- デプロイが簡単

### オプション3: Fly.ioを使用
Fly.ioはコンテナベースで、より柔軟です。

**メリット**:
- 完全な制御が可能
- FastAPIをそのまま使用可能
- グローバルデプロイ

### オプション4: Vercelで最小限版を使用
`api/index_minimal.py`を使用しますが、ファイルアップロード機能は制限されます。

**制限事項**:
- ファイルアップロードが複雑
- 機能が制限される

## 推奨
**Railway**を使用することを強く推奨します。FastAPIをそのまま使用でき、デプロイも簡単です。
